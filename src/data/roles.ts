import { tUserCredits } from './types'

export type tRole = {
    admin: tUserCredits,
    user: tUserCredits,
    admin_leader: tUserCredits,
    admin_projects: tUserCredits,
    curator: tUserCredits,
    expert: tUserCredits,
    admin_clubs: tUserCredits,
    head_office_manager: tUserCredits,
    admin_press: tUserCredits,
    admin_projects_read_only: tUserCredits
}
